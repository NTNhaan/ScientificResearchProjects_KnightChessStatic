using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class PlayerManager : MonoBehaviour
{
    void Start()
    {
        
    }

    void Update()
    {
        // if(TimeSliderHero.value == 0)
        // {
        //     TimeBar.ActiveSlider(TimeSliderEnemy, 0, isEnemyTurn);
        // }
        // if(TimeSliderEnemy.value == 0)
        // {
        //     TimeBar.ActiveSlider(TimeSliderHero, 0, isHeroTurn);
        // }
    }
}
